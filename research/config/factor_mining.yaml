# Round 1 全量：2024+2025 全部可扫描 spread
round_id: round_1
mode: full

data:
  data_dir: D:/data
  cross_dir: D:/data/跨品种
  dom_table_dir: D:/data/商品期货-主力合约
  years: ["2024", "2025"]

universe:
  calendar_products: []          # 空 = 扫描 data_dir/{year}/ 下全部品种
  cross_pairs: []                # 空 = 扫描跨品种/{year}/ 下全部配对
  max_spreads_per_product: 0     # 0 = 不限制

labels:
  horizons_min: [5, 15, 60, 240]

factors:
  lookback: 60
  vol_short: 20
  vol_long: 120

filters:
  require_trade: true
  max_eff_spread_ticks: 50

output:
  dir: research/output

selection:
  min_abs_ic: 0.03
  min_icir: 0.5
